# Build Log

Current active build: `v3.0-alpha`

The detailed configuration-controlled build history is maintained in [docs/BUILD_LOG.md](docs/BUILD_LOG.md).

This top-level file is provided so the project root contains the expected baseline artifact while keeping the detailed version history in the `docs/` directory.
