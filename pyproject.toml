[project]
name = "uuv-mission-planning-simulator"
version = "3.0.0a0"
description = "Research UUV mission planning and energy simulation tool"
requires-python = ">=3.10"
dependencies = [
  "gradio",
  "matplotlib",
  "numpy",
  "pandas",
  "requests",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
